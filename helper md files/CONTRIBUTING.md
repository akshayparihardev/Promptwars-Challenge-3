# Contributing

1. Run `pip install -r requirements-dev.txt` in the backend.
2. Run `npm install` in the frontend.
3. Ensure all tests pass (`pytest`) and linting is clean (`ruff check .`).
4. Submit a Pull Request.
